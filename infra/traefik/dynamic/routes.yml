# Traefik dynamic routes (T027). Update hostnames when DOMAIN changes.
http:
  routers:
    admin-ui:
      rule: Host(`api.mercflow.shop`) && PathPrefix(`/app`)
      priority: 100
      entryPoints:
        - websecure
      service: admin-ui
      tls:
        certResolver: letsencrypt
    medusa:
      rule: Host(`api.mercflow.shop`)
      priority: 1
      entryPoints:
        - websecure
      service: medusa
      tls:
        certResolver: letsencrypt
    grafana:
      rule: Host(`grafana.mercflow.shop`)
      entryPoints:
        - websecure
      service: grafana
      tls:
        certResolver: letsencrypt
    portainer:
      rule: Host(`portainer.mercflow.shop`)
      entryPoints:
        - websecure
      service: portainer
      tls:
        certResolver: letsencrypt
  services:
    admin-ui:
      loadBalancer:
        servers:
          - url: http://admin-ui:8080
    medusa:
      loadBalancer:
        servers:
          - url: http://medusa-backend:9000
    grafana:
      loadBalancer:
        servers:
          - url: http://grafana:3000
    portainer:
      loadBalancer:
        servers:
          - url: http://portainer:9000
