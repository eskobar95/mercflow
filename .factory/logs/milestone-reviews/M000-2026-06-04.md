# Milestone review — M000 Tenancy Foundation

**Date:** 2026-06-04  
**Branch:** `development` @ `28d3aa9`  
**Reviewer:** Cursor agent (harness)  
**Gate:** **Yellow** — code complete; Neon IP allowlist HITL open

---

## Summary

S001 (T001–T003) is merged. Technical tenancy, RLS, and rate limiting are implemented and tested in-repo. M000 cannot be **Green** until a human confirms Railway egress IPs are added to Neon `allowed_ips` (ADR-005, T003 HITL).

---

## Definition of done checklist

| # | Criterion | Result | Evidence |
|---|-----------|--------|----------|
| 1 | All 17+ tables have `store_id NOT NULL` + composite index | **Pass** | `Migration20260604120000AddStoreIdTenancy` (+ follow-up index migrations); DECISION LOG lists 11 MercFlow + 7 Guapo-custom |
| 2 | All 4 broken unique indexes rebuilt with `store_id` | **Pass** | `Migration20260604120000`, `Migration20260604200000`, `Migration20260604183000` |
| 3 | RLS + `tenant_isolation` on MercFlow tables | **Pass** | `Migration20260604220000EnableRlsTenantIsolation`; `test/tenancy-rls-migration.test.ts`, `test/tenant-scope.test.ts` |
| 4 | Rate limiting `429` on public + store routes | **Pass** | `apps/backend/src/api/middlewares.ts`; `test/rate-limit-middleware.test.ts` (7 tests green) |
| 5 | `pnpm migration:run` clean on local | **Pass (inferred)** | Migrations merged via PR #50/#52; content-module tenancy tests pass |
| 6 | `/milestone-review M000` green | **Yellow** | This review — blocked on HITL #7 below |

| 7 | Neon IP allowlist (T003 HITL) | **Open** | Human step — see `.factory/logs/hitl/M000-neon-allowlist.md` |

---

## PR traceability

| Task | PR | Merge |
|------|-----|-------|
| T001 | [#50](https://github.com/eskobar95/mercflow/pull/50) | `3997fb5` |
| T002 | [#52](https://github.com/eskobar95/mercflow/pull/52) | `a8436ea` |
| T003 | [#53](https://github.com/eskobar95/mercflow/pull/53) | `3f64f0c` |

---

## Verification commands (re-run locally)

```bash
pnpm --filter @mercflow/backend test
pnpm --filter @mercflow/content-module test
# Optional DB checks (Neon development branch):
# SELECT count(*) FROM article WHERE store_id IS NULL;
# SELECT tablename, policyname FROM pg_policies WHERE schemaname = 'public' AND policyname = 'tenant_isolation';
```

---

## Follow-ups to reach Green

1. Complete Neon allowlist HITL (human) — checklist in `.factory/logs/hitl/M000-neon-allowlist.md`
2. After HITL: set M000 status to `done` in `milestones.md` and mark T003 Neon checkbox `[x]`
3. S003 (T008) unblocks feed/sitemap tenant resolution — in progress separately
