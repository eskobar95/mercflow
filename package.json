{
  "name": "mercflow",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "pnpm --filter @mercflow/design-tokens build && pnpm --filter @mercflow/admin-ui build && pnpm --filter @mercflow/backend build",
    "build:admin-ui": "pnpm --filter @mercflow/admin-ui build",
    "build:design-tokens": "pnpm --filter @mercflow/design-tokens build",
    "dev:backend": "pnpm --filter @mercflow/backend dev",
    "migration:run": "pnpm --filter @mercflow/backend db:migrate",
    "migration:revert": "pnpm --filter @mercflow/backend exec -- medusa db:rollback --modules content",
    "lint": "eslint .",
    "test": "vitest run",
    "typecheck": "pnpm --filter @mercflow/design-tokens typecheck && pnpm --filter @mercflow/admin-ui typecheck && pnpm --filter @mercflow/content-module typecheck && pnpm --filter @mercflow/backend typecheck",
    "ci": "pnpm install --frozen-lockfile && pnpm lint && pnpm test && pnpm typecheck && pnpm build"
  },
  "devDependencies": {
    "@eslint/js": "^9.20.0",
    "eslint": "^9.20.0",
    "eslint-plugin-react-hooks": "^5.1.0",
    "eslint-plugin-react-refresh": "^0.4.19",
    "jsdom": "^25.0.1",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.24.0",
    "vitest": "^3.2.0"
  }
}
