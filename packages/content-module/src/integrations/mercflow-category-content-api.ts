/**
 * Re-exports the admin product-category content HTTP handlers for
 * `apps/backend` (see `mercflow-product-content-api.ts`).
 */
export { GET, POST } from "../api/admin/product-categories/[id]/content/route"
