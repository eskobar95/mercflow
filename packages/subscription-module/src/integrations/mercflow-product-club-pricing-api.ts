/**
 * Re-exports handlers for apps/backend (`src/api` is scanned only from backend root).
 */
export { GET, PUT } from "../api/admin/products/[id]/club-pricing/route"
export { DELETE } from "../api/admin/products/[id]/club-pricing/[variant_id]/route"
