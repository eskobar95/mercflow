import { PlaceholderPage } from "@/pages/PlaceholderPage"

export function ConnectorsPage(): JSX.Element {
  return (
    <PlaceholderPage
      title="Connectors"
      description="Connector configuration will ship in a later sprint."
    />
  )
}
